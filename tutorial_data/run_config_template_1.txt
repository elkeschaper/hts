# Add comments to your run config
experimenter = John Doe
experimenter_mail = john.doe@hts.org
plate_names = my_first_plate_name, my_second_plate_name, my_third_plate_name
base_path = /this/path/is/always/prepended


# Where is the data?
[readout]
    [[my_readouts_name]]
        path = local/path/to/my/raw/data
        filenames = experiment 1.csv, experiment 2a.csv, experiment_2b.csv
        format = csv
        delimiter = ','
    [[my_readouts_name_2]]
        path = local/path/to/my/raw/data_2
        filename = experiment2_{}.csv
        filenumber = a, b, c
        format = envision_csv
     [[my_readouts_name_3]]
         path = local/path/to/my/raw/data_3
         format = csv_one_well_per_row
         column_plate_name = plate_name
         column_well = plate_well
         columns_readout = readout_1, readout_2, readout_3
         columns_meta = extra_info_1, extra_info_2
         width = 24
         height = 16


# On the plate, what wells are samples, what wells are controls, etc.?
[plate_layout]
    path = local/path/to/plate_layout.csv
    format = csv

# How should we process the data (Quality control, Analysis)?
[protocol]
    path = local/path/to/protocol.csv
    format = config
